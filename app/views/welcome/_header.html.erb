<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">Daily</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item <%= active_class(root_path) %>">
        <%= link_to 'Home', root_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= active_class(welcome_news_path) %>">
        <%= link_to 'News', welcome_news_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= active_class(welcome_weather_path) %>">
        <%= link_to 'Weather', welcome_weather_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= active_class(welcome_tasks_path) %>">
        <%= link_to 'Tasks', welcome_tasks_path, class: 'nav-link' %>
      </li>
    </ul>
    <ul class="nav navbar-nav ml-auto">
      <li> 
        <% if signed_in? %>
        Signed in as: <%= current_user.email %>
        <%= button_to 'Sign out', sign_out_path, method: :delete, class: 'nav-link' %>
        <% else %>
        <%= link_to 'Sign in', sign_in_path, class: 'nav-link' %>
        <% end %>

        <div id="flash">
          <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
